config:
  target: "http://localhost:5656"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "search for songs"
    flow:
    - get:
       url: "/api/songs/search-song?query=jeffsatur"